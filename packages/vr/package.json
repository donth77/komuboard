{
  "name": "@komuboard/vr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Komuboard VR renderer (A-Frame + Three.js WebXR). M0 placeholder; built out in M4 (same Yjs doc as the 2D client).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@komuboard/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
